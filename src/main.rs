use meow::prompt::start;

fn main() {
    start().unwrap();

    // let mut v = meow::data::VecMinValue::Orig { vec: vec![1,2,3,4,5,6,7] };
    // v.compact();
    // println!("{:?}",v.iter().collect::<Vec<_>>())
}
